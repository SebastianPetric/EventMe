<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="thesis.hfu.eventmy" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
                android:name=".activities.RegistrationActivity"
                android:label="Registrierung" >
        </activity>

        <activity
                android:name=".activities.SearchActivity"
                android:label="Suche" >
        </activity>

        <activity
            android:name=".activities.CreateEventActivity"
            android:label="Event erstellen" >
        </activity>

        <activity
            android:name=".activities.AllEventsActivity"
            android:label="Alle Events" >
        </activity>

        <activity
                android:name=".activities.CreateTaskActivity"
                android:label="Aufgabe erstellen" >
        </activity>

        <activity
                android:name=".activities.AllTasksOfEventActivity"
                android:label="Alle Aufgaben" >
        </activity>

        <activity
                android:name=".activities.EventOrganizersActivity"
                android:label="Freunde um sie zum Event hinzuzufügen" >
        </activity>
    </application>

    <!--  Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
